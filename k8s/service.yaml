apiVersion: v1
kind: Service
metadata:
  name: sample-springboot-service
  labels:
    app: sample-springboot
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: sample-springboot
